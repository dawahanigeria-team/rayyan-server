const authController = require("./auth.controller");
const userController = require("./user.controller");
const fastController = require("./fast.controller");

module.exports = {
  authController,
  userController,
  fastController,
};
